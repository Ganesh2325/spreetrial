{
  "name": "spreetrail-platform",
  "version": "1.0.0",
  "description": "Root workspace workspace manager for Spreetrail shared expense platform",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "docker:db": "docker-compose up -d",
    "backend:dev": "npm run dev --prefix backend",
    "frontend:dev": "npm run dev --prefix frontend",
    "backend:migrate": "npm run prisma:generate --prefix backend && npm run prisma:push --prefix backend",
    "backend:seed": "npm run prisma:seed --prefix backend",
    "test:engines": "npx tsx scripts/test-engines.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "tsx": "^4.10.5",
    "typescript": "^5.4.5"
  }
}
